!start.

+!start <- .print("Hello world").
	
+value(N) <- .print("Updating belief value to ", N).

+lat(L) <-  .print("Updating belief lat to ", L).

+lon(L) <-  .print("Updating belief lon to ", L).

+teste(S) <- print("Updating belief lon to ", S).

+vetor(V) <- print("Updating belief vetor to ", V).
